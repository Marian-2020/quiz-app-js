# quiz-app-js
JavaScript quiz application that loads questions from a JSON file, renders answer options dynamically and stores user selections in localStorage. The project should be run through a local server environment such as Live Server in Visual Studio Code.
